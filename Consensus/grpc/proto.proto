syntax = "proto3";
option go_package = "ITUServer/grpc/proto";

message CriticalData {
    int64 Critcal_data = 1;
    int64 Time = 2;
}

message Accept {
    bool giveacces = 1;
}

message Empty {}

service Communcation {
    rpc Request (Critcal_data) returns (Accept) {};
}